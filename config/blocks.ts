export interface BlockMetadata { slug: string; title: string; description: string; category: string; engine: "recharts"; charts: string[]; exportName: string; fixtureName: string; fields: string; customization: string }
export const blocks: BlockMetadata[] = [{slug:"revenue-dashboard",title:"Revenue Dashboard",description:"Follow revenue from the monthly trend to the channels behind it.",category:"Revenue",engine:"recharts",charts:["line-basic","bar-basic","donut-basic"],exportName:"RevenueDashboard",fixtureName:"revenueDashboardData",fields:"Chronologically ordered records: month, channel, revenue (USD), orders. Revenue and orders must be finite and non-negative.",customization:"Replace the fixture, change the currency formatter, or compose the three chart panels separately. Period and channel selections filter the supplied records."}]
export function getBlock(slug: string) { return blocks.find(block => block.slug === slug) }
