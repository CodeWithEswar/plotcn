{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "chart-state",
  "type": "registry:ui",
  "title": "Chart State Fallbacks",
  "description": "Truthful loading, empty, and error fallback states with glassmorphic styling.",
  "dependencies": [
    "@hugeicons/react",
    "@hugeicons/core-free-icons"
  ],
  "registryDependencies": [
    "https://plotcn.vercel.app/r/chart-tokens.json"
  ],
  "files": [
    {
      "path": "components/charts/shared/chart-state.tsx",
      "content": "\"use client\"\nimport type { ComponentProps, ReactNode } from \"react\"\nimport { HugeiconsIcon } from \"@hugeicons/react\"\nimport { AlertCircleIcon, Database01Icon, Loading03Icon, UnavailableIcon } from \"@hugeicons/core-free-icons\"\nexport interface ChartStateProps extends ComponentProps<\"div\"> {title?:string;description?:string;icon?:ReactNode;action?:ReactNode;onRetry?:()=>void;retryLabel?:string}\nfunction State({title,description,icon,action,onRetry,retryLabel=\"Try again\",className=\"\",...props}:ChartStateProps){return <div className={\"plotcn-chart-scope flex h-full min-h-32 w-full flex-col items-center justify-center rounded-lg border border-dashed border-border p-5 text-center \"+className} {...props}><span className=\"mb-3 text-muted-foreground\">{icon}</span><p className=\"text-xs font-medium\">{title}</p><p className=\"mt-1 max-w-72 text-xs text-muted-foreground\">{description}</p>{onRetry ? <button type=\"button\" className=\"mt-3 rounded-md border border-border px-3 py-2 text-xs focus-visible:outline-2\" onClick={onRetry}>{retryLabel}</button>:action}</div>}\nexport function ChartLoadingState(props:ChartStateProps){return <State role=\"status\" title=\"Loading visualization…\" description=\"Preparing the visualization.\" icon={<HugeiconsIcon icon={Loading03Icon} size={18} className=\"motion-safe:animate-spin\"/>} {...props}/>}\nexport function ChartEmptyState(props:ChartStateProps){return <State role=\"status\" title=\"No data available\" description=\"There are no records for this selection.\" icon={<HugeiconsIcon icon={Database01Icon} size={18}/>} {...props}/>}\nexport function ChartErrorState(props:ChartStateProps){return <State role=\"alert\" title=\"Unable to load chart\" description=\"Check the data or try again.\" icon={<HugeiconsIcon icon={AlertCircleIcon} size={18}/>} {...props}/>}\nexport function ChartUnavailableState(props:ChartStateProps){return <State role=\"status\" title=\"Metric unavailable\" description=\"This visualization is unavailable for this selection.\" icon={<HugeiconsIcon icon={UnavailableIcon} size={18}/>} {...props}/>}\n",
      "type": "registry:ui",
      "target": "components/charts/shared/chart-state.tsx"
    }
  ],
  "categories": [
    "primitives"
  ]
}