import Link from "next/link"
import { SiteHeader } from "@/components/site/site-header"
import { SiteFooter } from "@/components/site/site-footer"
import { constructPageMetadata } from "@/lib/seo/metadata"
import { blocks } from "@/config/blocks"
import { BlockGalleryPreview } from "@/components/blocks/block-workspace"
export const metadata = constructPageMetadata({title:"Dashboard Blocks",description:"Composable dashboard source built from Plotcn charts and shadcn UI. Inspect working previews, data and dependencies.",path:"/blocks"})
export default function BlocksPage(){return <div className="charts-surface"><SiteHeader/><main className="lens-shell product-shell"><header className="product-heading"><div><span className="lens-eyebrow">PLOTCN / BLOCKS</span><h1>A dashboard. Already connected.</h1><p>Real charts, considered layouts, and source you own.<br/>Start with a composition. Make it fit your product.</p></div><span className="lens-eyebrow">{blocks.length} BLOCKS / SOURCE INCLUDED</span></header><div className="blocks-gallery">{blocks.map(block=><article className="block-gallery-card" key={block.slug}><div className="block-blueprint"><span>{block.category}</span><span>Recharts · {block.charts.length} charts</span></div><BlockGalleryPreview slug={block.slug}/><div className="block-gallery-caption"><div><h2><Link href={"/blocks/"+block.slug}>{block.title}</Link></h2><p>{block.description}</p><small>Interactive preview · sample data</small></div><Link className="block-open" href={"/blocks/"+block.slug}>Open & install</Link></div></article>)}</div></main><SiteFooter/></div>}
